const initialState={
    products:[{
        id:1,
        title:"Atefeh",
        type:"programmer",
    }]
}
export const productReducer=(state, action)=>{
    switch (action.type) {
        case value:
            
            break;
    
        default:
            break;
    }
}